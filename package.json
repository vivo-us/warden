{
  "name": "task-warden",
  "version": "1.0.0",
  "description": "Warden allows you to define taks that need to be only once or on a recurring schedule. It is built on top of a SQL persistence layer to ensure that tasks persist between restarts or failures. SQL was chosen for easy integration with exisiting infrastructure.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aaron Leman",
  "license": "ISC",
  "dependencies": {
    "cron-parser": "^4.3.0",
    "luxon": "^2.3.2",
    "mysql2": "^2.3.3",
    "sequelize": "^6.19.0",
    "winston": "^3.7.2"
  },
  "devDependencies": {
    "@types/luxon": "^2.3.1",
    "typescript": "^4.6.3"
  }
}
